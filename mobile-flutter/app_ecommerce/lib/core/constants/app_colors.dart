import 'package:flutter/material.dart';

class AppColors {
  // Texto
  static const textPrimary = Color(0xFF060606);
  static const textSecondary = Color(0xFF000000);

  // Botões
  static const bannerButtonBg = Color(0xFF080606);
  static const purchaseButton = Color(0xFF0828C9);
  static const purchaseIcon = Color(0xFFFFEDED);

  // Plano de fundo e surface genéricos
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);

  // Semânticas
  static const error = Color(0xFFB00020);
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF9A825);
}
